export const SERVICE_PACKAGE_NAME = "@droidproxy/service";

/** Placeholder until Fase 3 extracts backend orchestration from cli.js. */
export function isServicePackageReady(): boolean {
  return true;
}